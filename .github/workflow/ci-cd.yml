name: Java CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  use-reusable-pipeline:
    uses: https://github.com/uj-cpu-dev/deploy-to-aws-ecr/.github/workflows/ci-cd.yml